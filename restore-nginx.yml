- hosts: all
  become: yes

  tasks:
    - name: Ensure nginx installed
      apt:
        name: nginx
        state: present
        update_cache: yes

    - name: Ensure nginx running
      service:
        name: nginx
        state: started
        enabled: yes
